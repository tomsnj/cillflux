# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/tomsnj/cillflux
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/flux
  # Temporarily false while gotk-components.yaml is removed from this
  # path (2026-09-25). Flux computes pruning by diffing the previous
  # inventory against the new one, so shrinking the inventory with
  # prune enabled would garbage-collect the 11 Flux CRDs this
  # Kustomization shares with the `flux` Kustomization, cascade-
  # deleting every HelmRelease and Kustomization in the cluster.
  # Restored to true once the handover is verified.
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
