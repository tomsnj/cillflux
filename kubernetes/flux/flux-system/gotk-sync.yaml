# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/tomsnj/cillflux
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/flux
  # Was briefly false on 2026-09-25 while gotk-components.yaml was
  # removed from this path. Flux computes pruning by diffing the
  # previous inventory against the new one, so shrinking an inventory
  # with prune enabled deletes the difference -- and this Kustomization
  # shared the 11 Flux CRDs with the `flux` Kustomization, so that
  # would have cascade-deleted every Flux CR in the cluster. The
  # inventory has since settled at 32 objects, so re-enabling prune
  # has nothing to remove.
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
